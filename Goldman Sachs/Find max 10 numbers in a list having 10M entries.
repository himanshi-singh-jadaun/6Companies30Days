void solve (vector<int>nums)
{
  priority_queue<int, vector<int>, greater<int>>pq;
  
  for(int i=0;i<10;i++)
  {
    pq.push(nums[i]);
  }
  for(int i=10;i<n;i++)
  {
    if(nums[i]>pq.top())
    {
      pq.pop();
      pq.push(nums[i]);
    }
  }
  //print elements in min Heap
}
